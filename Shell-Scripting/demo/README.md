# Captured process output

[`demo`](demo) is a saved `ps aux` listing produced by the shell-script exercise. It shows the kind of data written to the generated file.

The process list is machine- and time-specific. Use [`../README.md`](../README.md) to run a fresh copy instead of treating this file as a current system snapshot.
